# State: Prime Agent autoresearch harness

## Config

- Scope: `research/autoresearch/` plus the scoped Prime subscription/provider fixes and campaign reports.
- Primary comparison: whole-frontier Pareto coverage over the two authoritative task vectors `(blowfish IR, bzip2 IR)`, lower is better. Raw tasks remain separate; no cross-task average is authoritative.
- Frozen reference: stock v3 measured `1,937 / 13,706` under verifier epoch `compiler-gym-v2`.
- Live host-owned arm: four provider requests, four candidates, eight fresh task evaluations, zero retries, and an intentional host stop before a fifth provider dispatch.
- Runtime: `openai-codex/gpt-5.6-luna`, `xhigh`, locally requested `priority`.
- Latest paid campaign disposition: proxy-cascade v1 and v2 are both terminal apparatus-invalid, and the campaign is closed after the second apparatus failure. V1 exposed a provider-response guard defect. V2 completed both online arms with eight subscription-backed Luna transports, eight tool calls, and 14 durable online measurements, then rejected its two post-terminal hidden audits because the host incorrectly required Slurm's `.extern` containment step to finish no later than the root job. Read-only `sacct` proves hidden allocations `1703771` and `1703772`, including their evaluator `.0` steps, completed `0:0`; only `.extern` lasted one second longer. V2 has `assessment=null` and `liveScientificEvidenceEligible=false`. Every online value is diagnostic only: no directional, frontier, resource-quality, replication, default, GPU, or NanoGPT claim is allowed, and there is no v3.
- Latest paid-apparatus qualification: the adapter now treats `.extern` as a Slurm containment/cleanup step while keeping the evaluator `.0` step strictly root-contained, records the external Slurm ID immediately, and durably records running/failed states, raw error artifacts, ordered aggregate children, and failure-path session seals. The paid matrix passes 60/60, the legacy proxy-cascade suite 53/53, the resource suite 37/37, and the repository check passes. A zero-model live qualification then ran two fresh concurrent bzip2 allocations, Slurm `1703787` and `1703788`; both exact root/extern/step triples completed `0:0`, overlapped, and passed all 20 semantic callbacks. This qualifies the repaired apparatus only and authorizes no paid dispatch or scientific claim.
- Latest completed paid pair: the separately preregistered stock-26 versus full-124 first-proposal visibility screen passed its shared zero-model S12 gate, then made exactly one subscription-backed Luna request and one tool execution per arm. The stock-26 control measured blowfish `1,960` IR / `22,380` object bytes and bzip2 `14,080` IR / `172,685` bytes. The full-124 treatment used 19 flags unavailable in the control guide but measured blowfish `1,971` IR / `23,040` bytes and bzip2 `14,268` IR / `181,907` bytes. Both were fully verified; control strictly Pareto-dominated treatment on the authoritative IR vector, so the frozen decision is `directional-loss` and the screen stops. Across the shared gate and two arms there were six unique FarmShare CPU allocations, two real provider calls, two tool executions, and one exact zero-token blocked continuation per arm. The requests locally asked for `priority`; the upstream response did not acknowledge a service tier. This single sampled-order pair permits no randomized or causal inference, default change, replication, GPU transfer, or NanoGPT promotion.
- Latest model-free systems result: the separately preregistered full-versus-proxy-cascade resource screen passed every frozen gate in two counterbalanced FarmShare CPU blocks. Block 1 ran full control then cascade: `84.477934 s / 198` evaluator-step CPU-seconds versus `54.664385 s / 136`. Block 2 reversed the order: cascade `52.682669 s / 126` versus control `82.121875 s / 188`. Median cascade/control wall ratio was `0.6443014367`, median absolute saving `29.6263775 s`, and median evaluator-step CPU ratio `0.6785407264`. Every arm reproduced the exact metrics, verifier results, frontier `[3]`, and champion 3; both cascade arms selected candidates `[2, 3]` solely from fresh blowfish measurements. The run used 28 fresh unique allocations, Slurm `1703626`–`1703653`, 84 exact root/extern/step accounting rows, eight local seals, ten remote locks, and maximum evaluator concurrency two, with zero failures, retries, replacements, reuse, models, providers, tools, RLM, compaction, or GPUs. This order-balanced model-free feasibility result qualified one separately preregistered paid directional campaign; that follow-up was attempted and closed apparatus-invalid. The model-free result makes no agent-performance, default, GPU, KernelBench, or NanoGPT claim.
- Preceding model-free systems qualification: the production-default proxy-cascade runner completed its one allowed prospective eight-allocation FarmShare CPU audit. Four fresh blowfish measurements selected candidates 2 and 3; their two fresh bzip2 measurements formed the cascade, and two hidden audit-only bzip2 measurements checked omitted candidates 1 and 4. The complete frontier remained `[3]` and champion 3 was retained. All eight Slurm IDs, job names, and transient caches were unique; all 24 root/extern/step accounting rows were `COMPLETED/0:0`, the terminal queue was empty, and there were zero failures, retries, replacements, reused measurements, model/provider calls, RLM children, or GPU allocations. The run consumed 102 allocation-wall seconds, 196 requested task CPU-seconds, and 408 scheduler logical CPU-seconds. It qualifies live production-default wiring only: six versus eight is a counterfactual 25% allocation-count reduction, not an observed compute, time, latency, agent, default, GPU, or NanoGPT improvement.
- Best measured vector in the earlier full-versus-concise feedback pair: full-feedback typed control at `1,925 / 13,676`; this is one directional observation, not a promoted harness feature.
- Latest scientific-policy result: paid IR-delta screen v2 terminated in its randomized first treatment arm after a 47-action proposal violated the frozen 46-action cap. Three real Luna dispatches and four fresh task evaluations produced two verified candidate vectors, but no control arm or pair comparison; the feature did not qualify for promotion or GPU transfer.
- Earlier paid apparatus result: a separately preregistered late-novelty screen v2 made exactly one subscription-backed Luna dispatch and returned the frozen S12 tool request, then its first evaluator allocation failed before producing recoverable raw output. A later read-only `sacct` observation identified Slurm `1703304`, `FAILED/3:0`; that scheduler identity is not embedded in the sealed local artifact. Treatment randomized first, no accepted measurement or control arm exists, and the sealed attempt is terminal apparatus-invalid with no retry or GPU promotion. Compaction and RLM were disabled for this isolation attempt.
- The preceding late-novelty v1 stopped before any attempt lock, provider dispatch, or evaluator allocation. A contemporaneous console observation identified different duplicate source-path closures; that thrown text is not persisted in the one-event sealed ledger. The setup failure was repaired and regression-tested only for the fresh v2 namespace; v1 remains unchanged.
- Preceding mechanism result: the one-environment IR-delta evaluator passed its fresh eight-allocation model-free qualification across S12/L46 and blowfish/bzip2. Every pair preserved exact terminal metrics and the 20-input verifier; median treatment/control runtime was `0.996886`, the maximum was `1.021607`, and the aggregate native-rendered projection was `2,039` bytes / `19.6587%`. That qualification authorized only the now-terminal paid screen and made no Luna-benefit claim.
- Later decision-support work did not change that scientific status: broad Pareto-summary injection failed a model-free headroom gate at `3/21 = 14.2857%`, while a development-informed late-novelty reconstruction and four-call faux run qualified only the prompt-wiring apparatus, not a live treatment.
- FarmShare snapshot, observed `2026-08-28 20:47–20:50 PDT`: 13 of 24 L40S devices were physically unallocated, but `srun --test-only` predicted an approximately eight-hour GPU wait; a small CPU request was immediately schedulable. The GPU QoS allowed four jobs and four GPUs. This is time-sensitive scheduler state, not immutable campaign evidence.
- FarmShare recovery snapshot, observed `2026-08-29 17:27 PDT`: zero-model Slurm job `1703328` passed locally with the frozen operational environment seal plus all 20 semantic callbacks; read-only `sacct` reported `COMPLETED/0:0` on `barley-01` in ten seconds, and the queue was empty. The seal covers the declared package, compatibility, bitcode, runtime, cBench, and library manifests; it is not a byte-complete hash of every installed Python file. Two earlier diagnostics isolated missing Gym sources and a truncated compatibility tree. The old extracted files' timestamps and deletion pattern make nightly scratch cleanup the leading explanation, but that cause is inferred rather than directly observed.
- GPU snapshot, observed `2026-08-29 17:43 PDT`: a one-L40S ten-minute `srun --test-only` projected start at `2026-08-31 15:55 PDT`, roughly 46 hours later. No GPU job was submitted. CPU recovery therefore does not make the GPU lane synchronous.
- Latest NanoGPT scored-apparatus result, observed `2026-08-30`: the production verifier epoch `nanogpt-scored-v1-ec824abe9c0ec43a5645afdb` binds the exact static evaluator, environment manifest and seal, 19-file dataset, worker, and transport bytes. The byte-exact stock candidate passed its candidate-specific 10-step smoke as Slurm `1703802`, `COMPLETED/0:0`, in `2m43s`: seed `0xC0FFEE`, final validation loss `6.485082150`, ten optimizer steps, and peak VRAM `29,634.344 MB`. The manifest requested eight CPUs; FarmShare accounting distinguishes `ReqTRES cpu=8` from the actual `AllocTRES cpu=10`. Request `826a1cb90d9523b4c5342cba7a6a706d70710e2489e121302cba102e33781bb9`, result `6a9a465282037d80bdf19420f2e9bbde49b197181a21daf3bec4b0a51a29f880`, and receipt `dbf0d16fa9ce34ac8059e9113e3028eb34eda1c34ffbe9d5fc72bd2e165f6af4` reverified through a fresh live reconcile with no redispatch. This admits the apparatus and stock smoke only; the full one-seed score is running separately as Slurm `1703804` and is not yet a result.
- Durable-environment bootstrap: an additive, hash-verifying home-bundle builder and 5/5 local regression suite are ready. Contemporaneous remote command output, not persisted in a campaign artifact, reported `EDQUOT` while creating the parent before a staging directory or switch was created. The active environment therefore remains the recovered scratch tree; no home migration is claimed.
- Latest zero-model headroom result: the complete-action-space v1 runner stopped before preflight, lock, scheduler query, or `srun` because its trusted-read request exceeded the backend bound; its ledger records zero dispatches and remains preserved. The corrected fresh v2 attempt dispatched exactly once as Slurm `1703490`. Its raw evaluator payload completed the exact 98-flag dijkstra sweep and passed its internal headroom gate with `-early-cse-memssa` (`292 -> 260`) and `-early-cse` (`292 -> 269`), each passing 20/20 callbacks. The host did not admit that payload: root `AllocCPUS=4` violated the frozen exact value `2`, although the actual step remained one CPU/one task and completed `0:0`. The 29-event ledger ends terminal apparatus-invalid, the scientific lock forbids a rerun, and the raw signal is non-admitted exploratory decision evidence only.
- Prospective paid-dispatch safety: late-novelty runner/preregistration protocol v2 now executes the frozen read-only login-node environment probe before every provider transport. Each pass or failure is recorded in the host-only pair ledger with arm and dispatch ordinal; failure leaves provider transport, provider counters, downstream guard, and evaluator work at zero. This requires a fresh preregistration and does not alter any existing paid lock or verifier epoch.
- Branch: `codex/async-autoresearch-harness`; campaign work remains uncommitted.

## Playbook

### Interface and evidence isolation

- [P1] Compare complete Pareto frontiers, not only an assistant-selected scalar champion. The V4 stock arm contained a non-selected vector that strictly dominated typed even though its scalar selection looked mixed.
- [P2] Keep authoritative measurement details immutable while varying only the model-visible projection. The concise-feedback pair did this and preserved all verifier evidence.
- [P3] Force fresh CompilerGym measurements in live comparisons. Reuse is useful infrastructure but is not a physical evaluator call.
- [P4] Treat Luna trajectories as unseedable directional units. One matched pair may kill a preregistered pilot but cannot establish a causal effect.
- [P5] Close trajectories in the host after the fixed candidate budget. Assistant `CHAMPION` text is advisory and cannot create an extra paid turn.

## Tried

| Iter | Approach | Result | Learning |
|---|---|---|---|
| 0 | Frozen stock v3 reference | reference | Four synchronous candidates reached `1,937 / 13,706` under the strict verifier. |
| H1 | Sealed typed measured-recall `M` | discard | Exact quality tie used more total tokens and bundled several interface differences. |
| H2 | Controlled width `W` | discard | Lower critical-path time did not preserve quality. |
| H3 | Retest/resurrection `R` | discard | The frozen 2x2 evaluator gate found zero conditional benefit. |
| H4 | Sparse sharing `S` | unrun | Its preregistered prerequisites did not pass. |
| 1–2 | Typed synchronous parity | inconclusive | V1 passed the frozen target; unchanged V2 missed it, consistent with unseedable trajectory variance. |
| 3 | Native stock versus typed whole-interface screen | inconclusive | Three valid pairs split typed-positive, stock-positive, and scalar-mixed; the V4 whole-Pareto sensitivity favored stock. |
| 4 | Concise evaluator-feedback projection | discard | Model-visible feedback fell 86.5% and modeled cost 20.0%, but full feedback strictly dominated concise on both tasks. |
| 5 | Fuse both tasks into one allocation | discard before implementation | The tasks already run concurrently; bzip2 was critical in all 69 frozen campaign-cohort pairs, and the optimistic historical median saving was only `0.731 ms`. |
| 6 | Reuse one warm allocation versus fresh warm roots | mechanism-only keep | Two sealed blocks produced 8/8 candidate wins, median saving `16.033 s` and `25.65%`, exact metrics, and clean accounting; this did not compare against stock cold `srun`. |
| 7 | Actual stock cold `srun` versus persistent warm transport | discard | The no-model matched pair preserved exact requests, jobs, metrics, verifier evidence, lineage, and budgets, but warm took `194.673 s` versus cold `124.793 s` and missed the 10% gate. |
| 8 | Sealed C1 v2 job-step qualification | terminal infrastructure discard | The one allowed held root was submitted, but strict parsing rejected Slurm's exact one-node range `NumNodes=1-1` before release or candidate publication. Native cleanup recovery hit the same frozen assertion; exact manual cancellation verified `CANCELLED`, scheduler absence, and zero allocated CPU time. No replacement is permitted. |
| 9 | Shadow action-trace qualification v1-v3 | terminal infrastructure discard | V1 stopped before allocation on an absent remote source parent; v2 stopped after one allocation on stale environment paths; v3 completed all eight allocations with exact metrics/verifiers but the frozen parser rejected `-mergereturn` because CompilerGym reported no module modification while observed IR increased by one. No v3 result was admitted and no retry is permitted. |
| 10 | One-environment IR-delta cost/equivalence smoke | mechanism-only keep | Four fresh ABBA allocations preserved exact L46/blowfish metrics `1,981 / 23,028` and 20/20 verification. Treatment traces telescoped `3,898 - 1,917 = 1,981`, including `-mergereturn: +1`; median total-runtime ratio was `0.935880` and maximum `1.038765`. Promote only to the fresh model-free qualification. |
| 11 | One-environment IR-delta formal qualification | mechanism-only keep | Eight fresh plan-ordered stock-cold allocations preserved exact canonical/treatment metrics and 20/20 verification across four S12/L46 cases. Median runtime ratio was `0.996886`, maximum `1.021607`, and the model-visible projection added `2,039` bytes / `19.6587%`. Promote only to a separately sealed directional Luna pair. |
| 12 | Paid IR-delta directional screen v1 | terminal apparatus invalid | One paid Luna dispatch returned the exact frozen S12 request, and two fresh FarmShare tasks reproduced `1,970 / 21,501` and `13,838 / 166,545` with all 20 callbacks passing. The host rejected Python float lexemes such as `3.6e-05` against JavaScript's equivalent `0.000036` before delivering feedback; no adaptive proposal, control arm, comparison, or treatment result exists. |
| 13 | Paid IR-delta directional screen v2 | terminal scientific policy nonconformance | Three paid treatment-arm responses produced valid 12- and 25-action candidates, then a 47-action proposal violated the preregistered 46-pass cap and was rejected before evaluator dispatch. The raw runner result incorrectly labels the induced local abort as apparatus failure; independent ledger/session adjudication establishes durable policy nonconformance. No control arm or pair comparison exists. |
| 14 | Broad late-stage Pareto-summary injection | discard before implementation | A model-free development screen found only `3/21 = 14.2857%` headroom, below the declared 20% minimum. No artifact, module, evaluator job, or model call was created. |
| 15 | Ordinal-four structural-novelty reconstruction | faux-only headroom | Fourteen eligible transitions were reconstructed from the frozen 21-ledger latency cohort. Low Dice-LCS similarity had 5/7 dominating outcomes versus 2/7 for high similarity, a 42.857-point gap, but post hoc Fisher sensitivity and strong protocol confounds restrict this to faux-prompt motivation. |
| 16 | Four-call late-novelty prompt wiring | apparatus-only keep | Both faux arms made four provider, evaluator, and tool calls. Only treatment call four saw the bounded guidance, authoritative raw artifacts were byte-identical, and there were no duplicates or compactions. This is no treatment result. |
| 17 | Shared GPU-lane capacity | apparatus-only keep | The controller now caps aggregate KernelBench plus NanoGPT work at four running jobs. A faux regression held a fifth GPU job, allowed CPU progress, then admitted and merged the delayed job exactly once after capacity released. |
| 18 | Late-novelty paid screen v1 | terminal setup invalid | One sealed preflight event exists and no attempt lock, result, provider, or evaluator evidence follows. A contemporaneous console observation identified a duplicate implementation source path; the exception text itself was not persisted. The fix used only a fresh namespace. |
| 19 | Late-novelty paid screen v2 | terminal apparatus invalid | One Luna dispatch returned the exact frozen S12 tool request with zero duplicate dispatches. The first treatment evaluation failed; later external `sacct` showed `1703304`, `FAILED/3:0`, but the old adapter discarded the underlying stdout/stderr and local Slurm identity. No accepted measurement or control exists, and the paid lock forbids repetition. |
| 20 | Raw evaluator-error evidence hardening | apparatus-only keep | Future typed adapter failures keep model-visible errors sanitized while persisting host-only evidence and raw stdout/stderr as content-addressed run-manifest artifacts. The new module is included in sealed source inventories; it cannot recover v2's missing streams. |
| 21 | FarmShare environment recovery diagnostics | apparatus-only keep | Zero-model jobs `1703309` and `1703313` exposed missing Gym sources and a `1,247/2,749` compatibility tree. After restoring pinned archives, `1703328` accepted empty-pass blowfish at `3,898 / 16,573` and passed the frozen operational seal plus 20/20 callbacks; external `sacct` reported `COMPLETED/0:0`. |
| 22 | Per-dispatch live environment admission gate | apparatus-only keep | Prospective runner/preregistration v2 executes and records the frozen read-only FarmShare probe before each paid transport. Faux failure leaves provider and evaluator dispatch at zero; successful records are ordinal-bound and terminal accounting matches passes to actual paid calls. |
| 23 | Durable CompilerGym home bundle | blocked before staging | The additive hash-verifying builder passed 5/5 local tests. Contemporaneous unsealed remote output reported `EDQUOT` at parent creation; no stage, switch, or active-path change occurred, so scratch remains authoritative. |
| 24 | Complete legal action-space headroom | terminal apparatus invalid; exploratory raw signal | V1 made zero Slurm dispatches after a local trusted-read bound mismatch. Fresh v2 swept all 98 omitted flags in one zero-model job; the raw payload found two 20/20-verified dijkstra improvements, but root `AllocCPUS=4` violated the frozen exact `2`, so no result or claim event was admitted and no rerun is allowed. |
| 25 | Stock-26 versus full-124 first-proposal visibility | directional loss; stop | The shared zero-model S12 gate exactly reproduced `1,970 / 13,838` IR and object sizes `21,501 / 166,545`, with 20/20 callbacks on both tasks. Control measured `1,960 / 14,080` IR and `22,380 / 172,685` object bytes; treatment used 19 guide-omitted flags but measured `1,971 / 14,268` IR and `23,040 / 181,907` bytes. Both were fully verified, but control strictly dominated the authoritative IR vector. The pair used two Luna calls, one tool call per arm, exact zero-use blocked continuations, and six unique CPU allocations total. No randomized/causal/default/GPU/NanoGPT claim is allowed. |
| 26 | Prospective blowfish-proxy cascade qualification | apparatus-only keep | A sealed post hoc replay over 13 four-candidate trajectories retained every full two-task frontier while reducing task allocations by `17.3077%` and summed evaluator runtime by `28.5178%`; it authorized only a model-free wiring audit. The production-default audit then completed eight fresh unique CPU allocations: four blowfish, bzip2 for selected candidates 2 and 3, and hidden bzip2 audit for omitted candidates 1 and 4. Frontier `[3]` and champion 3 were retained with zero failures/retries/reuse/model/provider/RLM/GPU work. The actual run used all eight allocations; six versus eight supports only a counterfactual 25% allocation-count statement, not an actual compute/time/latency or agent-performance claim. |
| 27 | Counterbalanced full-versus-proxy-cascade resource screen | model-free systems keep | Two AB/BA blocks reproduced exact metrics, verifiers, frontier `[3]`, and champion 3. Cascade selected `[2, 3]` from fresh blowfish only and used six versus control's eight allocations in each block. Median wall ratio was `0.6443014367`, median saving `29.6263775 s`, and median evaluator-step CPU ratio `0.6785407264`. All 28 fresh unique allocations and 84 accounting rows passed with maximum concurrency two and zero failures/retries/reuse/model/tool/GPU work. This qualified one separately preregistered paid directional campaign; that follow-up was attempted and closed apparatus-invalid. No agent/default/GPU/NanoGPT claim follows. |
| 28 | Paid proxy-cascade directional campaign v1-v2 | terminal apparatus invalid; campaign closed | V1 exposed a provider-response guard defect. Fresh v2 completed both online arms with eight Luna transports, eight tool calls, and 14 verified online measurements, but the host rejected two post-terminal hidden audits after their `.extern` rows outlived root by one second. Read-only accounting proves all 16 allocations and evaluator steps completed `0:0`; the defect was host-side admission, not scheduler failure. The preregistered second-apparatus-failure stop closes the campaign: assessment remains null, every online value is diagnostic, and no v3 or scientific comparison is permitted. |
| 29 | Proxy-cascade accounting and failure-provenance repair | apparatus-only keep | Role-correct accounting no longer constrains `.extern` end time but still strictly contains `.0`; external IDs are recorded immediately, and failures retain running/failed states, raw artifacts, aggregate children, and session-integrity seals. The paid matrix passed 60/60, legacy proxy-cascade 53/53, resource 37/37, and the full repository check passed. A fresh zero-model live qualification ran overlapping jobs `1703787` and `1703788`; both exact accounting triples completed `0:0` and passed 20/20 callbacks. This validates apparatus only, not either paid campaign or a scientific treatment. |
| 30 | Production NanoGPT scored verifier and stock smoke | apparatus-only keep | The six-key epoch pins evaluator, environment manifest/seal, dataset, worker, and transport; dispatch is no-requeue and fsynced, acceptance requires exact `sacct COMPLETED/0:0`, and receipts bind the job script, scheduler row, result, and reverified archived logs. Stock smoke job `1703802` passed seed `0xC0FFEE` for 10/10 steps at loss `6.485082150` and peak VRAM `29,634.344 MB`; the manifest requested eight CPUs while FarmShare allocated ten, and a fresh reconcile recovered the same receipt without redispatch. This is an apparatus/lineage gate, not a full score or frontier point. |

## Current evidence

- Matched interface summary: `.autoresearch/stock-interface-pair-screen/summary-v1.json`; immutable correction: `.autoresearch/stock-interface-pair-screen/summary-v1-correction.json`.
- Host-owned historical replay: `.autoresearch/stock-interface-pair-screen/host-terminalization-replay-v1.json`; all seven measured arms retained the same selected candidates and measurement qualification.
- Feedback pair preregistration: `.autoresearch/feedback-projection-screen/preregistration.json`, SHA-256 `35e3ebc238ed6420345add651bdaa48c80d2590eb046addc601442e96345d7b5`.
- Feedback analysis: `.autoresearch/feedback-projection-screen/analysis-v1.json`, SHA-256 `a3217038330388f205da95d1866d26bcccce618bf1b77585ec47e1af2cf0b2c4`.
- Full feedback frontier: `1,925 / 13,676`. Concise frontier: `1,962 / 14,103`. Full covers concise `1/1`; concise covers full `0/1`.
- Concise/full feedback-byte ratio: `0.134572`; total tokens changed by `-32.1%`, input tokens by `-54.2%`, and modeled cost by `-20.0%`.
- Both arms passed strict ledger, artifact, source, projection, budget, duplicate, and post-terminal checks: four provider requests, four evaluator calls, eight fresh tasks, zero retries, and one host stop each.
- The sealed arm-local preregistrations inherited stale five-call fields. The earlier controlling pair preregistration and observed runtime require four; the discrepancy is disclosed and sealed artifacts are unchanged.
- Warm mechanism preregistration: `.autoresearch/compiler-gym-warm-reuse-pilot/preregistration-v1.json`, SHA-256 `36d1b74086fbf72b3a0d577c33ef0f00f7d92eedc8b24597d6d33e3019a81c53`; execution-v6 passed its mechanism-only gate.
- Stock transport preregistration: `.autoresearch/stock-interface-transport-pair/preregistration-v1.json`, SHA-256 `eb79cd9878fed9cbc61d960abdf4bb29c1b3c4cf6e9584712e4e780a78c6ff92`; runtime SHA-256 `5c110801b143ebf0be196ca7f06e9b1cd852ef57a5d6ac2c03ab5d90631c9535`.
- Stock transport result: `.autoresearch/stock-interface-transport-pair/execution-v1/result.json`, SHA-256 `91cb497a66a711c9be7d0be539512b9c1dce3afa182abeaeaeb96d4c2b0de82d`; cold `124.792743916 s`, warm `194.672781209 s`, delta `+69.880037293 s`.
- All 52 arm-ledger events and 26 content-addressed artifacts reverified. Warm root `1701997` ended `COMPLETED/0:0`, with four CPUs, `CPUTimeRAW = 4 x ElapsedRaw`, acknowledgements `[0,1]`, and an empty final queue.
- Job-step C1 v2 preregistration SHA-256 is `97873fc4d862e3d81388430c260c0f84e9e1ca75e78be4be7588ede0750dc381`; runtime-manifest SHA-256 is `0c993c5e28d6eaf2d47f69abecd98071d3203a3495dd659b4543f88091e87feb`. BatchMode authentication and the required preflight passed before dispatch.
- The sealed runner submitted held root `1702093` exactly once, then rejected `NumNodes=1-1` against its frozen exact-string `1` assertion before releasing the root or publishing a candidate. The immutable execution ledger contains five events; `failure.json` records the post-submit terminal failure. There was no replacement, provider call, candidate step, task evaluation, CPU allocation, or GPU use.
- Sealed cleanup-only recovery was invoked once and hit the same parser assertion. Exact external cleanup then ran `/usr/bin/scancel --full 1702093`; `squeue` verifies absence and `sacct -X` records `CANCELLED`, `ExitCode=0:0`, `AllocCPUS=0`, `ElapsedRaw=0`, and `CPUTimeRAW=0`. The sibling manual-cleanup attestation does not rewrite the native `rootCleanupVerified=false` result.
- Slurm documents `NumNodes` as `min_count[-max_count]`; therefore `1-1` is a representation of the exact requested bound, not evidence that multiple nodes were requested. This identifies a strict parser defect but does not rescue or permit repetition of the frozen experiment.
- The campaign record now covers iterations through 30. Iteration 30 is the accepted production NanoGPT scored-apparatus smoke; the separate full score-1 job remains pending and contributes no result. Bundle currency is established only by `campaign-report-bundle.ts --write` followed by `--check`; the generated HTML and hash manifest must not be inferred current from this narrative alone.
- The report records the terminal C1 v2 failure, action-trace and IR-delta apparatus gates, both immutable paid-screen outcomes, the late-stage model-free/faux screens, the production NanoGPT scored protocol/adapter/transport/worker/stock driver, the accepted smoke job `1703802`, and the pending/non-evidence status of score-1 job `1703804`.
- The original faux-only combined-ledger regression proved that generic per-lane pumps could keep KernelBench and NanoGPT pending, complete CompilerGym work, then merge delayed GPU measurements in reverse order exactly once. The newer shared-capacity regression adds a hard four-job aggregate cap across both GPU lanes while preserving CPU progress and one-time measurement merge. Both are local operational evidence, not a live combined campaign. Source: [`controller.ts`](research/autoresearch/src/controller.ts) and [multi-lane regression](research/autoresearch/test/multi-lane-controller.test.ts).
- The unpersisted broad Pareto-summary development screen was killed at `3/21 = 14.2857%`, below its 20% headroom gate. It produced no sealed artifact, implementation, evaluator job, or model call; its only durable disposition is this state/log entry.
- The late-novelty audit reverified the pinned [21-ledger latency input](.autoresearch/compiler-gym-latency-audit/analysis-v1.json) and reconstructed 14 ordinal-four transitions. With `S = 2*LCS/(|child|+|parent|)`, the low-similarity band (`S < 0.80`) had 5 dominates, 0 improves-neither, and 2 tradeoffs; the high-similarity band had 2, 4, and 1, respectively. The domination-rate gap was `42.857` percentage points. Source and regression: [headroom analyzer](research/autoresearch/src/compiler-gym-late-novelty-headroom.ts) and [test](research/autoresearch/test/compiler-gym-late-novelty-headroom.test.ts).
- The corresponding Fisher sensitivity is weak: one-sided `491/3432 ~= 0.143`, two-sided approximately `0.286`. The threshold and selection were development-informed; only 14/21 ledgers were eligible; the cohort mixes optimization with deliberately deleterious `R` diagnostics; the remaining-protocol sign depends on that diagnostic and reverses when it is removed; first-declared-parent binding misattributes one resurrection mechanism; and structural similarity is not a causal intervention. The reconstruction authorizes faux wiring only: no provider, paid, treatment, promotion, or GPU claim.
- The four-call faux pilot added one 128-byte serialized guidance field only to the third accepted treatment result, so only treatment provider call four saw it. Each arm made four provider, evaluator, and tool calls, with zero duplicates, one host stop, zero compactions, and byte-identical authoritative artifacts. Source and regression: [guidance](research/autoresearch/src/compiler-gym-late-novelty-guidance.ts), [pilot policy](research/autoresearch/src/compiler-gym-late-novelty-pilot.ts), and [faux E2E](research/autoresearch/test/compiler-gym-ir-delta-screen-faux-e2e.test.ts). This validates apparatus only.
- Late-novelty paid v1 preregistration/evidence SHA-256 values are `3ad488c6a0a0a2f6b1ed6c50ced0015cc90634e07d1310eb5618d5697703aabb` and `4a3b66a57e81e8056d81417eabc53437c0da0e3b1f781456411b797dbd0ce4d9`. Its single preflight ledger event and absence of a lock/result establish zero paid or evaluator work. The duplicate-source cause comes from contemporaneous console output, not persisted sealed evidence.
- Late-novelty paid v2 preregistration/result/pair-ledger SHA-256 values are `b643267bbca810684b93557096fa67d095b402cbd77af09bb44e90166caa79c5`, `c628d537766dfad025a9592fc8cd01fa9e068734676db8d8bcf531120428d7bd`, and `49da646f9f772d7afa9148bb61d9be037de1ca1dff6805c01cecf6d61d89f352`. The arm ledger and session hashes are `5738b1f4af84f2771027ff934962da8616e10de0ee192c1efa8a16bf6c64ce43` and `078bc92d63dfff1222d31ba3e7d2fda7e245e651c217947b835d10eac0d79f31`; the global lock remains `79b73799fb23022d0dc38f3c95e82d6256a5d8243371702c330521d1dc247001`.
- V2's one real provider dispatch used `1,899` input and `233` output tokens (`2,132` total; modeled cost `$0.006594`). The provider payload requested `priority`; the upstream response API exposed no tier acknowledgement. The exact frozen S12 tool request, request anchor, model snapshot, source snapshot, and zero duplicates all matched. This is clean model/tool-routing evidence, not a treatment result.
- The v2 adapter reduced the failed evaluation to a generic 65-byte message and did not retain Slurm's raw stdout/stderr. Future adapter failures now carry typed sanitized model-visible errors while storing raw streams as host-only content-addressed artifacts; the new module is included in the preregistration, stock, and transport source closures. This repair is not retroactive and does not recover v2's missing bytes.
- Zero-model recovery result/ledger SHA-256 pairs are v1 `1e165dd0ebafeb51d5ec3abf5172dfee8f390be56c96388e0ef645b37c2d48a9` / `6180035c2e455f614da72229a0576b64b8ab41181963d7957883cc602f0d8bf1`, v2 `8f30b268fa5a8266f8c255e34ce7c121902ebf8cc52e887ffa6cbd94dbb17cc3` / `1dfc7258c78b94d6f18bf3afa87d8a2f71eac795730522ac96d28570083cd6ca`, and v3 `51eb3155c6ce4bd8354e67c642495b6f4df40b5ec2538075f3d5d6f0e96735ae` / `13669c9c53e836b59a34d351bcabb118439f357e02457ad3b1486619f12f3da6`. These are apparatus diagnostics with zero model calls, not scientific candidates.
- Prospective paid runner/preregistration protocol v2 seals the exact environment-probe source and expected stdout, runs it over read-only login-node SSH with `/usr/bin/env -i` and no `srun`/`sbatch`, and records ordered pass/fail evidence before the hardened provider guard. The full late-novelty suite passes 41/41; no live model, SSH, Slurm, lock, or evidence state was touched by those tests.
- Home-bundle builder/test SHA-256 values are `c2ee638750042ed41a675354499f7b39738fb0787e9e55b33a5e4820dd1d54d3` and `e39d052361211ce2c4f2fac0f07975049a0bc47fb52bb7b2cc2639650c799d4b`. Contemporaneous remote output, not persisted in a campaign artifact, reported `EDQUOT` before stage creation. The hashes prove a local migration mechanism, not a completed or active FarmShare install.
- Complete-action-space v1 preregistration/ledger SHA-256 values are `8b730a9e1067cfa4d741af17dcdb6cfd5004cea8f46f7d0bdddb85497f330cee` and `2bed13b1cfa9f95aff26a7f0f11cf130abea27d46d787183e1469624385d01e5`; its terminal event records `dispatchAttempts=0`, no cleanup need, and no scientific lock or scheduler history. V2 preregistration/ledger/lock SHA-256 values are `c5205ae82c81ff7b14dc8e77130d1b1065a2362503d29f368dd878ee212f5f7c`, `e679d4d41d2b03bc7db93794b41cb088053ec9687c4db6f0fbd2ea4c6f86ea3d`, and `dde529b60ac9a9751f5146b595753ff0d790cf143779aacaa765f06fbe5d6297`. The v2 ledger embeds content-addressed evaluator stdout, stderr, and `sacct` artifacts, records one dispatch and cleanup proof, and ends failed rather than complete.
- The v2 raw evaluator payload SHA-256 is `73dc6ffb921da7a6e85b117f53be75252c3ece290bb0111b9efafaddf2213cd8`. A local read-only replay through the frozen result assessor returns `gatePassed=true`, five raw qualifiers, verified flags `-early-cse-memssa` and `-early-cse`, `59.933243` CPU seconds, and `61.128304` wall seconds. This does not override native non-admission. Embedded `sacct` records root/extern `AllocCPUS=4`, step `AllocCPUS=1`, `NTasks=1`, and `COMPLETED/0:0`; a later read-only scheduler diagnosis found `ReqMem=8G`, `MaxMemPerCPU=4000`, and whole-core allocation, explaining the four-CPU root reservation without changing the frozen contract.
- Visibility-pair preregistration/result/pair-ledger SHA-256 values are `30c6b86d2cd043ed766fb3ff97b9c8179c9a664529fd6af95039f05aeb323024`, `f113e6ec8bba8cadf9ee2165205bda61802a7145074ab9da40c2f48d50419ccc`, and `9d2babb8e90ffffaae64d624dc8d2ffbe1a016020cb87c754c508550f7f0497a`. The shared-S12, control-evaluation, and treatment-evaluation ledger hashes are `48f10ac972b9b714c7bbb6996944d309d3de642932763d7cb7eaf0e0dffc2fa4`, `edece3771988b155f43f49dc1bd866f8f933ac44b729625629e2681e9ea2ecd1`, and `578346149cb01f3bb87720c0675fc3bf37770721da5e061d891377b31054c130`.
- The visibility run's exact raw aggregate SHA-256 values are shared S12 `37814430137bbd27c15092f184632149f923e5b05b2a04f1538529acfe379e72`, control `f2a20a38dd77f8518afb64c37143a6113d59d5c38c3fe986fc581e3ce9c49f0e`, and treatment `38924f9624efc596939f034bb264a90dfb62bdb6e08f8d6fdbe225acbbce8d6d`. Slurm allocations were `1703515`, `1703516`, `1703519`, `1703520`, `1703521`, and `1703522`; all six were unique. Both arms contain exactly one transmitted paid assistant response, one successful tool execution, and one blocked `provider-call-limit` continuation whose durable assistant usage is exactly zero.
- The treatment uniquely used 19 flags from the guide's omitted-98 suffix, so the mechanism engaged, but it lost both authoritative tasks. The result's `randomizedInferenceAllowed=false`, `causalClaimAllowed=false`, `defaultPromotionAllowed=false`, `gpuPromotionAllowed=false`, and `nanogptPromotionAllowed=false` fields are controlling. No repeat, interaction, GPU transfer, NanoGPT run, or default change is authorized.
- Proxy-cascade replay result/manifest SHA-256 values are `824065b538e0eb38436f32eb981970497ecf3d64a815a4b8dafcc1f559bd79d2` and `1d3904b367e455f271276bb7fbe13762de3de062b1d0dfa2f7b98960d020607c`. The frozen post hoc cohort contains 13 trajectories; all frontiers were retained, task allocations changed from 104 to 86 (`17.3077%`), and summed evaluator runtime changed from `947,686.128` to `677,427.105 ms` (`28.5178%`). The replay explicitly disallows provider, paid, treatment, default, GPU, NanoGPT, and feedback-latency claims.
- Prospective proxy-cascade preregistration/result/ledger SHA-256 values are `9a76b71cb66724182c8d393469a3dd403e211449460a3f31cb163cb03dd41da3`, `11ead1ff072cad2dd01ade41ef9ad2b362e14da9aab04573e4a0a543d7fc9edb`, and `d27b5e85bf7d221eab88b856e2f310c9575810b531c79e70933b71d91f5e8446`; the terminal event is `3bf8544b224303980990ee3a58f36aeae860b7b9945c0064c8aa39e512fb11ac`. The exact eight-allocation audit retained frontier `[3]` and champion 3 under scientific identity `55e461a30e2a3f874fffc09fd0cc0b4a7c663c08033953eca633cd4b8041044b`.
- Counterbalanced resource-screen preregistration/result/ledger SHA-256 values are `554e3530a3adc5bbe800cce69eb06bdf501823d394e835734e90e2371029513e`, `894d444f7059ac8c301eee8b4167d88fa0314d2f9f4c345873f56cb6ad7e862f`, and `21d0e379151b1c4618f96adea41974490cdc064ebfd3a850e0cb07d4c4b2af6f`; the terminal event is `0d2aac8cb65ec72996299099a4807a9ac67c9e2c6c8396c8ce9d947172fe0715`, under scientific identity `22d838d4c144732c1cfcf064a55f65f61b1eea619b826a31ee42dbc0c67cb45c`. Both order-balanced blocks passed the frozen wall, evaluator-step CPU, exact-allocation, metric, verifier, frontier, champion, and non-slowdown gates. This is resource feasibility for one possible paid pilot, not a treatment result.
- Proxy-cascade paid v1 preregistration/result/ledger SHA-256 values are `1c016ddff62f6556464febb8ae8a4fb0026600542e195a950cc9e4ee21d09e95`, `0b59060a1df32587eaaab336a91466b0bbb740aadf5d8e860d2bda2857b910e5`, and `538b9c906eb1c7e357faa1caacee8a87dc009fe3f7e012f20fd7f14368ec456c`. Its terminal identity is consumed as apparatus-invalid after the response guard rejected a valid provider shape; the repaired parser passed exact replay and adversarial malformed cases without changing the sealed attempt.
- Proxy-cascade paid v2 preregistration/result/ledger SHA-256 values are `0c6ab6b481e5482499eae591bb970535c332cb602b170321c97c7eb6df372ee5`, `98077f929e90169a18754fce8c06401605ee29e3ede19c3e672a01dac6fc4e85`, and `1b218d60ebac1e4c54ff10058a841afe5a42f3b2ed846b55d19d3cb9b2ef6985`; the terminal event is `26f8206fe2483e0c315d54d8eb2bb6143021092ff6ff412f1d7626e0d2cdceea`, under scientific identity `7522261ba2c357f3e89e9bbe272a7374b578975e3784a7d88ce943035747cc0e`. Treatment then control completed eight paid transports, eight tool calls, and 14 online measurements. Hidden jobs `1703771` and `1703772` physically completed, but admission failed because `.extern` ended one second after root; therefore `assessment=null`, `liveScientificEvidenceEligible=false`, and all online metrics are diagnostics only.
- The repaired paid apparatus passed 60/60 focused tests, 53/53 legacy proxy-cascade tests, 37/37 resource tests, and the full repository check. Its fresh zero-model live qualification result/ledger/seal SHA-256 values are `eb5ecece523916d6712e68e2e50f343da151ebf59625cc9841ce50c08ed53694`, `6c19d682332d49def1e9d579496fb0bc312c33fd2abf42e8d44e3de5854a02f6`, and `1913bed248864a3ab80facd39138b996f2af4b2e35ae199ed2b4c6148e1e861e`; the ledger terminal event is `0798a2290151482f064a7eed47edc25c3b5d87450c1218cfb67ddd88368ee123`, under qualification ID `3ce586d3-4b89-4e21-9f55-6ae012903a34`. Jobs `1703787` and `1703788` overlapped, each passed exact root/extern/step accounting and all 20 callbacks, and used zero model/provider/session calls. This is apparatus evidence only.
- The three lanes are a gated funnel, not interchangeable benchmarks: CompilerGym is the fast CPU harness screen, KernelBench-Verified is optional GPU verifier/transfer stress after a CPU promotion, and NanoGPT Track 3 is the direct confirmatory estimand. A production scored NanoGPT adapter and authoritative stock smoke now exist, but there is not yet a completed full stock score, GPU treatment transfer, or record-eligible frontier point. The older [ten-step result](.autoresearch/nanogpt-farmshare-smoke/2026-08-28-v1-sealed/result.json) and [ledger](.autoresearch/nanogpt-farmshare-smoke/2026-08-28-v1-sealed/evidence.jsonl) remain separate unscored infrastructure evidence only.
- The historical frozen campaign config remains immutable at SHA-256 `31f6b78ac5c5ccb87a471e61bbad46e1f0d1b1c892eb7c678c1e8f5a8e02e2f4` with widening schedule `[1,2,4,8]`. Before the first scored dispatch, prospective amendment `0aece581a2cc9e49672f598129a9f298353114368962a4fca0e09b0f7e9751ab` defined a separate unscored `smoke-10` gate followed by scored stages `[1,3,8]` (`score-1`, `score-3`, `replay-8`). It changes only interim widening, preserves the final eight-seed rule, and forbids historical reclassification or cross-protocol aggregation.
- Action-trace v3 preregistration SHA-256 is `a4956d205b7b0ec548e289958bf15a7c290de1c3ba7723b14d31ca42a56609ca`; its terminal ledger SHA-256 is `5b97f945005709276459eb9f344a3f63ce315d599fad3bb4642a6710d2d9f688`. All eight allocations completed, but the frozen raw no-effect/IR-delta contradiction made the attempt terminal infrastructure-invalid with no admitted result.
- IR-delta smoke preregistration/result/ledger SHA-256 values are `a2b6ff3136e4d88217871d57bb65f7b1c6b526ea0fe89a54e73e4600ea9da3be`, `4d750e7fcafa7b3f8a03f8779c7649d539773fbfa1a830f06d5fac7bc4286237`, and `a180f587d458529557e5d8900e875a5b1ab07e18bf307ff0c8cb5c60966cdf98`. Independent audit reverified 29 linked events, 18 physical artifacts, four unique completed Slurm jobs `1702131`-`1702134`, four unique caches, exact source hashes, pre/post probes, accounting, queue absence, and zero retries/replacements/model calls.
- The smoke's first canonical allocation was cold-cache inflated, while the reverse-order warmed block still passed at `1.038765`. Treatment action observation added about `16.2 ms` on the long trace, so the promoted qualification must include S12 and L46 across blowfish and bzip2. The smoke makes no agent-benefit, latency, benchmark-generality, or causal claim.
- Formal IR-delta qualification preregistration/result/ledger SHA-256 values are `c9be9a6c4cace30a903c9f698f32e1e234a62de344f4a62a88c9163ee2580a33`, `060631a72ed582749fc823cb8a3da0fc0975938442343a1c6639576d50613208`, and `e7dec53e1673af776901601ea94343d42b1c939131c7cb7c68ffeb87ba2fe3b3`; the terminal event is `50e5bcc9cbde5b7ecf5bf444a825443b0aa269d4aaaaa7b806a111c40a20cdbe`.
- Four independent audits reverified the 42-event strict ledger, 55 content-addressed artifacts, 51-file source closure, one dispatch lock, eight unique Slurm jobs and caches, exact plan order/accounting/probes, empty terminal queue, trace telescoping, pair equivalence, and projection reconstruction. All allocations were `COMPLETED/0:0`; there were zero retries, replacements, reused measurements, or model calls.
- Formal per-case treatment/control total-runtime ratios were `0.839522`, `0.985174`, `1.021607`, and `1.008598`. The first canonical allocation was colder, but excluding that pair leaves median `1.008598` and maximum `1.021607`; the preregistered pass is robust to the visible cold-cache asymmetry.
- Paid screen v1 preregistration SHA-256 is `c423cf047ac0495b0812b413a39e622cbdc8ebcb9bd0664aee20cde55a1e2422`; its terminal pair result, pair ledger, and terminal event hashes are `c4bdea863e0954b6d0f091ebbf09ccd30588ec810b7a8da3e2c6f804bd45a27d`, `8089c67e45916ddf9948ef4c3c9635d7f4ddac2b303419e5b956a603d52cca0e`, and `257712bb496eeebb18e789b8e1663c3448d65982872233f2d7cd7d90fa9a5ff7`. The sealed disposition is `terminal-apparatus-invalid-not-treatment-result`.
- V1 made one subscription-backed `openai-codex/gpt-5.6-luna` request at `xhigh` with locally requested `priority`: `1,885` input, `237` output, and `2,122` total tokens. It launched one evaluator job containing two fresh tasks (Slurm `1702176` and `1702177`) and made zero retries, reused measurements, adaptive requests, control-arm requests, GPU allocations, RLM calls, web calls, or compactions.
- Both v1 S12 measurements and all verifier callbacks were valid. The raw stdout SHA-256 values are `07a2d80b848100e68e94a58d8958285d26a25c2609f17febf3485dde233d5798` and `1eacbbdd3f9d71f52934dea764af6726bb51477ad7616654488adbb70e11d40f`; the aggregate artifact SHA-256 is `60c61a5b9ded3b2895d3068459cb3c3f341d3b22f6e207f583d6590bb1ff9447`.
- The v1 parser failure is a representation mismatch, not metric, verifier, trace, scheduler, hash, provider, or tool-routing drift. Python's compact sorted JSON uses exponent spellings for small floats where JavaScript `JSON.stringify` uses decimal spellings. The host aborted before the valid measurement could become model-visible feedback, so all induced incomplete-count failures are downstream apparatus symptoms rather than scientific-policy evidence.
- The strict numeric-lexeme repair passed the exact sealed v1 aggregate model-free: both tasks reconstructed the complete treatment projection and produced trace SHA-256 `521c0e8e168197e33923512bc02c9dad794854f40ecd6a261e8df110c7e22146`. Independent adversarial replay rejected whitespace, key-order, string-spelling, hash, schema, telescope, lossy-number, negative-zero, underflow, unsafe-integer, and non-finite mutations.
- Paid screen v2 preregistration/result/pair-ledger SHA-256 values are `0b822e4f37d57bcb55544ef2b82183f147cf7a0bd1f35593d62469388e096488`, `f79f2d0ccb6fd1b69fdeda5304856afca46b955cc9898a9a20215e3f1d3acab5`, and `2168ff61fc71ae8570823feec728c617ea7625cf29b67bc9a70c272be88d9868`; the terminal pair event is `d46da98b0fa4cc650508a5fcca08961748d77463f7820b6374ddb337a9a4990b`. The treatment arm result/session/ledger hashes are `4f7cce562b1ec244a9d1929ddfd510bd7225f42896f19d2af6e5627faffcbe3f`, `89f1c6dbf6979b888b98c61f7d10ccb0ef7f835da3d6218e0daaeadc13807736`, and `e814754e6923ccdab7148bb44dd76eb79a66b8e9d80d9e6a966df3794411a058`.
- V2 made three paid Luna dispatches, used `7,678` input, `2,573` output, `5,120` cached-input, and `15,371` total tokens, and recorded modeled cost `$0.047256`. It launched four unique fresh task evaluations with no reuse or duplicate: S12 reproduced `1,970 / 13,838`, and the 25-action adaptive candidate improved both IR counts to `1,958 / 13,751`; every admitted task passed all 20 callbacks.
- V2's third model response proposed exactly 47 actions after receiving two treatment trace envelopes. The host rejected it locally under `scientific-policy-nonconformance: actions must contain from 1 through 46 flags`; prospective digest `b056e26b4e349b25cf9ff7fe79cf376161e8a7bca95a86ab8d14ab531ac59efa` appears in neither ledger nor artifact store. The following zero-token aborted assistant record was synthesized by the intentional provider block and was not a fourth paid dispatch.
- The sealed v2 result's `terminal-apparatus-invalid-not-treatment-result` label is a future-audit bug, not permission to retry. Its two listed apparatus failures are both induced accounting artifacts: the expected synthetic local abort was treated as a provider failure, and four durable assistant records were compared against three real provider dispatches. The preregistered taxonomy controls: more than 46 actions is terminal scientific-policy nonconformance and durable negative policy evidence.
- A read-only replay of the exact sealed v2 trajectory under the corrected future-audit taxonomy returns `terminal-scientific-policy-nonconformance`, `admitted=false`, zero apparatus failures, three provider dispatches, two evaluator jobs, four fresh task evaluations, and the same two verified candidate vectors. The replay changes no sealed byte or hash and does not authorize a retry.

## Next gate

- Let the exact one-seed full stock score finish and reconcile before any `score-3` widening. Widen only if the completed score passes the fixed `mean loss < 3.27859` threshold; the running Slurm job is not evidence.
- Do not replicate concise feedback and do not transfer it to GPU; its preregistered quality kill fired.
- Do not implement per-candidate pair fusion. Halving allocations does not remove the critical bzip2 allocation's setup, and the all-history optimistic model fails the proposed `>=2 s` and `>=10%` wall-saving gate.
- Do not run the warm agent-facing Luna screen: the matched stock transport gate failed before paid dispatch.
- Do not rerun or replace sealed C1 v2, and do not run its C1–C4 screen. Its post-submit failure is terminal under the preregistration, so candidate-scoped job-step transport is killed.
- Parser hardening may accept only semantically exact one-node encodings such as `1` and `1-1` while rejecting wider ranges, but that is a future correctness repair. It cannot alter the frozen C1 disposition or justify a retry.
- Any further CPU experiment must be a genuinely different, preregistered stock-cold hypothesis in a fresh namespace. No current agent-facing feature qualifies for GPU transfer.
- Do not rerun the completed formal IR-delta qualification. It passed once under its stable attempt lock and admits only its terminal result.
- Never rerun, rewrite, append to, or present paid screen v1 or v2 as a completed pair. V1 is terminal apparatus evidence; v2 is terminal scientific-policy evidence. Neither reached the hidden-control arm.
- Any later paid experiment must test a genuinely distinct hypothesis under a new name, preregistration, namespace, source/runtime closure, random draw, attempt lock, and output path. It cannot be described as completing, replacing, or rescuing v2.
- Never rerun, replace, or append to paid screen v2. It is a terminal treatment-arm scientific-policy failure with no control arm and therefore cannot support a trace-effect comparison, replication claim, default promotion, GPU transfer, or NanoGPT run. Correct the induced-abort taxonomy only for future audits; sealed v2 bytes remain authoritative historical evidence.
- The future model-visible screen tool contract is now aligned with the authoritative runtime rule: stock remains `maxItems: 256`, while the opt-in screen schema requires `minItems: 1` and `maxItems: 46`, retains the runtime guard, and binds the exact schema into pre-transport provider parity. This hardening does not rescue v2.
- Any later paid pair would remain directional and could not by itself authorize a causal claim, default feature promotion, GPU transfer, or NanoGPT treatment run. Compaction was disabled in the IR-delta isolation attempts; its separate sealed live canaries already passed.
- Never rerun, rescue, retrospectively admit, or place the complete-action-space v2 payload on a verified frontier. Its exploratory raw signal motivated exactly one genuinely new, separately preregistered stock-26 versus full-124 prompt-visibility pair with corrected scheduler accounting. That pair is now complete and its `directional-loss` stop decision is terminal for this pilot; it authorizes no repeat, default change, GPU transfer, NanoGPT work, randomized inference, or causal claim.
- Do not implement the broad Pareto-summary idea: its model-free 20% headroom gate failed. Never rerun, replace, append to, or describe either late-novelty paid attempt as a completed pair. V1 is terminal pre-provider setup evidence; v2 is terminal treatment-first apparatus evidence with one paid dispatch, no accepted measurement, and no control. Neither authorizes a treatment claim, promotion, GPU transfer, or NanoGPT work.
- Before any distinct future paid CPU hypothesis, require one successful zero-model Slurm smoke under the same frozen environment. The prospective v2 runtime then gates every provider dispatch on a fresh read-only login-node probe before transport. Static package-version hashes alone are insufficient because the FarmShare purge left metadata present while package and dataset files were missing.
- Treat the four-job shared GPU cap as an in-process safety bound for the current one-GPU-per-job lanes. Slurm QoS remains authoritative across other processes, and the time-stamped queue observation must be refreshed before scheduling work.
- The proxy-cascade paid v1/v2 campaign is closed under its second-apparatus-failure stop rule. Never rerun, replace, append to, or retrospectively admit either attempt; v2's online observations are diagnostics only and support no pair, frontier, resource-quality, replication, default, GPU, or NanoGPT claim.
- The repaired accounting and failure-provenance path plus live zero-model jobs `1703787` and `1703788` satisfy only the future apparatus prerequisite. They authorize no paid call. Any new paid campaign requires separate user authorization and a genuinely new hypothesis, protocol, name, scientific identity, random draw, namespace, locks, sessions, source closure, and output paths; it cannot reuse v1/v2 candidates, measurements, or history as model-visible search input.
- The proxy-cascade wiring qualification and the distinct counterbalanced resource screen remain complete and exact-once. The resource screen supports an observed model-free schedule/resource saving under its fixed four-candidate CPU design; its one authorized paid campaign was attempted and closed without an admissible comparison. It does not establish an agent-performance effect or authorize a default change, KernelBench transfer, GPU claim, or NanoGPT work.
- Keep the report bundle green with `npx tsx research/autoresearch/src/campaign-report-bundle.ts --check`; no report claim may outrun its pinned local evidence.
- Continue to withhold KernelBench and NanoGPT treatment runs until an agent-facing CPU feature passes its promotion gate.
